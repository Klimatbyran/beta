apiVersion: v1
kind: Service
metadata:
  name: klimatkollen-frontend
  namespace: klimatkollen
  labels:
    app: klimatkollen-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: klimatkollen-frontend